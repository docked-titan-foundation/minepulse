{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-empty": [2, "never"],
    "scope-enum": [
      2,
      "always",
      ["mp", "ci", "deps", "dependencies", "docs", "test", "build", "security", "release", "repo"]
    ]
  }
}
